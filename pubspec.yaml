name: Khaldiya
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.3
  carousel_pro: ^1.0.0
  url_launcher: ^5.7.8
  share: ^0.6.5+3
  flutter_launcher_icons: ^0.8.1
  http: ^0.12.2
  cached_network_image: ^2.4.1
  get_mac: ^0.0.1
  shared_preferences: ^0.5.12+4
  get:


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_icons:
    android: "launcher_icon"
    ios: true
    image_path: "asset/Image/logoapp.jpg"


flutter:
  uses-material-design: true
  assets:
     - asset/Image/

  # example:
  fonts:
     - family: Almarai
       fonts:
         - asset: asset/Font/Almarai-Regular.ttf


     - family: BebasNeue
       fonts:
         - asset: asset/Font/BebasNeue-Regular.ttf